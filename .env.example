# Your site address
NEXT_PUBLIC_SITE_URL=

# Baidu analytics id retrieved here: https://tongji.baidu.com/
NEXT_PUBLIC_BAIDU_TONGJI=
# Google analytics id retrieved here: https://analytics.google.com/
NEXT_PUBLIC_GOOGLE_ID=

# Neon PostgreSQL (Vercel Postgres / Neon integration)
DATABASE_URL=
DATABASE_URL_UNPOOLED=

# Opa! Suite API
OPA_BASE_URL=https://opa.accessnet.com.br/api/v1
OPA_TOKEN=
OPA_CANAL_WHATSAPP_ID=
# IDs de template e departamento ficam hardcoded em lib/opa/constants.ts

# Opa! Suite webhook — secret compartilhado pra validar requests vindos do Opa!
# Configurar no painel do Opa! como header `x-opa-webhook-secret` ou query
# `?secret=`. Endpoint: POST /api/webhook/opa
OPA_WEBHOOK_SECRET=

# Lead capture — rate limit per IP per minute
LEAD_RATE_LIMIT_PER_MIN=10

# Playwright E2E tests (dev only)
PLAYWRIGHT_BASE_URL=http://localhost:3000
E2E_TEST_PHONE=